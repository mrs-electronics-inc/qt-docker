# `builder` image

This image does the dirty work of installing Qt toolchains so that they can be used as layers of other images.

Required external Python dependencies:

- [`aqtinstall`](https://github.com/miurahr/aqtinstall): CLI to unofficially install Qt
